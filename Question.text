Question
意味わからん。なんで動いてんのかわからん。
なんで急にsetStepとかいう関数がちゃんと動いてんのやーーー

  const [step, setStep] = useState(1);

  function handlePrevious() {
    if (step > 1) setStep(step - 1);
  }
  function handleNext() {
    if (step < 3) setStep(step + 1);
  }